#include "point.h"

int main(){
  Point2d point;
  Point2d point2;
  //Constructs point and copies it into point
  point = CreatePoint2d(5, 5);
  point2 = CreatePoint2d(0, 0);
  PrintPoint(point);
  PrintPoint(point2);
  return 0;
}
