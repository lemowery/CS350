#include "node.h"
#include <stdio.h>
#include <stdlib.h>

Node* CreateNode(const void* data_to_store){
  Node* node = (Node*) malloc(sizeof(Node));
  if (node == NULL){
    return NULL;
  }
  node->data = data_to_store;
  node->previous = NULL;
  node->next = NULL;
  return node;
}
