#include <stdio.h>

//argc => argument counter (array size)
//argv => argument vector (array of strings)
int main(int argc, char** argv){
  printf("Argc: %s", );
  printf("First: %s", argv[0]);
  printf("Second: %s", argv[1]);
  printf("Third: %s", argv[2]);
  return 0;
}
